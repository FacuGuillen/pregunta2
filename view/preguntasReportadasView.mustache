<h1>Preguntas Reportadas</h1>

<p class="mb-4 text-muted">
    A su vez puede revisar las preguntas reportadas, para <strong>aprobar</strong> o <strong>dar de baja</strong>.
</p>

<ul class="list-group">
    {{#preguntasReportadas}}
        <li class="list-group-item d-flex justify-content-between align-items-center">
            <span>{{pregunta}} ({{categoria}})</span>

            <div class="d-flex gap-2">
                <!-- Aprobar pregunta -->
                <form action="/editor/aprobarReportada" method="POST" class="mb-0">
                    <input type="hidden" name="id_pregunta" value="{{id_pregunta}}">
                    <button type="submit" class="btn btn-sm btn-success">Aprobar</button>
                </form>

                <!-- Dar de baja -->
                <form action="/editor/rechazarReportada" method="POST" class="mb-0">
                    <input type="hidden" name="id_pregunta" value="{{id_pregunta}}">
                    <button type="submit" class="btn btn-sm btn-danger">Dar de baja</button>
                </form>
            </div>
        </li>
    {{/preguntasReportadas}}

    {{^preguntasReportadas}}
        <li class="list-group-item">No hay preguntas reportadas para mostrar.</li>
    {{/preguntasReportadas}}
</ul>

<!-- Botón para volver al perfil del editor -->
<div class="mt-4">
    <a href="/editor/show" class="btn btn-outline-secondary">Volver al perfil del editor</a>
</div>
